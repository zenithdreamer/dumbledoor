# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL of Prisma ORM
USER_DATABASE_URL='postgresql://user:password@localhost:5432/dbname?schema=public&sslmode=disable&pgbouncer=false'
ACCESS_DATABASE_URL='postgresql://user:password@localhost:5432/dbname?schema=public&sslmode=disable&pgbouncer=false'
DOOR_DATABASE_URL='postgresql://user:password@localhost:5432/dbname?schema=public&sslmode=disable&pgbouncer=false'
DATABASE_URL='postgresql://user:password@localhost:5432/dbname?schema=public&sslmode=disable&pgbouncer=false'

# Ports
USER_SERVICE_PORT=4000
ACCESS_SERVICE_PORT=4001
DOOR_DATABASE_PORT=4002

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://next-auth.js.org/configuration/options#secret
AUTH_SECRET='supersecret'
INTERNAL_API_SECRET='supersecret'